spring.application.name=web-crud
# change port of application/ tomcat server
server.port = 8082

spring.messages.basename = mymessages
spring.messages.encoding = UTF-8

# database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/pes
spring.datasource.username=root
spring.datasource.password=Razak@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql: true
# create>> replace the structure
# update>> append
spring.jpa.hibernate.ddl-auto=update